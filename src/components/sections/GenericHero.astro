---
interface Props {
  classes?: string;
}

const { classes = "overflow-hidden bg-sunny-500 py-56 pb-32 flex items-center relative" } = Astro.props;
---

<section class={classes}>
  <div class="container mx-auto px-4 ">
    <slot />   
  </div> 
</section>

