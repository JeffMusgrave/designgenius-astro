<!-- ---
import Layout from '../layouts/Layout.astro';
import { getCollection } from 'astro:content';

const blogPosts = await getCollection('blog');
---

<Layout title="Blog - Design Genius">
  <section class="py-12 md:py-20">
    <div class="container mx-auto px-4">
      <h1 class="text-4xl md:text-5xl font-heading mb-8">Blog</h1>
      <div class="grid gap-8 md:grid-cols-2 lg:grid-cols-3">
        {blogPosts.map((post) => (
          <article class="bg-white dark:bg-gray-800 rounded-lg overflow-hidden shadow-lg">
            <a href={`/blog/${post.slug}`}>
              <img src={post.data.image} alt={post.data.title} class="w-full h-48 object-cover" />
              <div class="p-4">
                <h2 class="text-xl font-semibold mb-2">{post.data.title}</h2>
                <p class="text-gray-600 dark:text-gray-300">{post.data.excerpt}</p>
              </div>
            </a>
          </article>
        ))}
      </div>
    </div>
  </section>
</Layout> -->